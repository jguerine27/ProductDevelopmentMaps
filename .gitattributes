# Normalise to LF in the repo. The VPS is Ubuntu; a CRLF that reaches a shell
# script or container entrypoint fails there as "bad interpreter: /bin/sh^M".
* text=auto eol=lf

# Binary assets must never be line-ending converted.
*.png binary
*.jpg binary
*.xlsx binary
*.zip binary
